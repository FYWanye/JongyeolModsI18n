fix(editor): 双击可用的交互式菜单；修复 BOM 污染与计数不准

双击没反应的两个原因：
- 无参数运行时只打印帮助就退出，控制台随进程关闭（"一闪而过"）
- 双击时工作目录是 exe 所在目录（target/release），只从 cwd 找仓库根会失败

修复：
- 无参数运行进入交互式菜单（进度总览 + 1 拉取原文 / 2 逐条汉化 / 3 查看进度 /
  4 上传 / 5 覆盖本地 / 6 查看配置），退出前暂停等回车；被重定向时不暂停
- workspace_root 增加「从 exe 所在目录向上找」与环境变量 I18N_EDITOR_REPO
- Windows 下把控制台切到 UTF-8(65001)，避免中文乱码

顺带修两个真实缺陷：
- 输入未消毒：管道/粘贴带来的 UTF-8 BOM 会被当成译文写进 JSON（实测已复现并修复）
- changed 计数统计的是「赋值次数」而非真实内容变化；现按内容差异统计，
  且无改动时不重写文件
